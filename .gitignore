# ---------- 运行时生成，不进仓库（云上由数据源冷启动重建 + Supabase 承载人工字段）----------
data/
uploads/*.tmp
*.pyc
__pycache__/

# ---------- 本地/日志 ----------
app.log
server.log
*.pid

# ---------- 部署敏感信息（用 Render 环境变量注入，绝不提交）----------
.env
